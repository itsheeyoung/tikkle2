<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="heeyoung.tikkle2">

    <uses-permisson android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signupactivity" />
        <activity android:name=".loginactivity" />
        <activity android:name=".bene_signup_activity" />
        <activity android:name=".setting_activity" />
        <activity android:name=".supporter_home_activity" />
        <activity android:name=".board_writing_activity" />
        <activity android:name=".board_reading_activity" />
        <activity
            android:name=".supporter2_home_activity"
            android:label="@string/title_activity_supporter2_home_activity"></activity>
    </application>

</manifest>